services:
  monitor:
    build: .
    volumes:
      - ./:/app
    working_dir: /app
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
    init: true
    command: ["python3", "src/main.py"]
    pid: "host"